<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <style>
    .login_form {
      width: 300px;
      margin: 0 auto;
      font-family: Arial, Helvetica, sans-serif;
    }

    .login_form h1 {
      text-align: center;
      color: #4d4d4d;
      font-size: 24 px;
      padding: 20px 0px 20px 0px;
    }

    .login_form h3{
        text-align: center;
        color: rgb(230, 76, 76);
        font-size: 20px;
    }

    .login_form input[type="text"],
    .login_form input[type="password"] {
      width: 100%;
      padding: 15px;
      border: 1px solid #ddd;
      margin-bottom: 15px;
      box-sizing: border-box;
    }

    .login_form input[type="submit"] {
      width: 100%;
      padding: 15px;
      background-color: #525b63;
      border: 0;
      box-sizing: border-box;
      cursor: pointer;
      font-weight: bold;
      color: white;
    }

    .login_form a[href="/register"]{
      text-decoration: none;
      color: #4d4d4d;
    }
  </style>
</head>
<body>
  <div class="login_form">
    <h1>Login Page</h1>
    <% if (locals.message) {%>
    <h3>-<%= message %></h3>
    <% } %>
    <form action="/auth" method="post">
      <input type="text" name="username" placeholder="Email/Username" value="<% if (locals.user) {%><%= user %><% } %>" required>
      <input type="password" name="password" placeholder="Password" required>
      <input type="submit" value="Login">
    </form><br>

    <a href="/register">Register</a>
  </div>
</body>
</html>