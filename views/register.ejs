<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <style>
    .register {
      width: 300px;
      margin: 0 auto;
      font-family: Arial, Helvetica, sans-serif;
    }

    .register h1{
      text-align: center;
      color: #4d4d4d;
      font-size: 24px;
      padding: 20px 0px 20px 0px;
    }

    .register h3{
        text-align: center;
        color: rgb(230, 76, 76);
        font-size: 20px;
    }

    .register input[type="text"],
    .register input[type="password"],
    .register input[type="email"] {
      width: 100%;
      padding: 15px;
      border: 1px solid #ddd;
      margin-bottom: 15px;
      box-sizing: border-box;
    }

    .register input[type="submit"] {
      width: 100%;
      padding: 15px;
      background-color: #525b63;
      border: 0;
      box-sizing: border-box;
      cursor: pointer;
      font-weight: bold;
      color: white;
    }

    .register a[href="/login"]{
      text-decoration: none;
      color: #4d4d4d;
      margin: 0;
      text-align: left;
    }
  </style>
</head>
<body>
  <div class="register">
    <h1>Register Page</h1>

    <% if (locals.message) {%>
        <h3>-<%= message %></h3>
    <% } %>

    <form action="/auth/register" method="post">
      <input type="email" name="email" placeholder="Email" value="<% if (locals.email) { %><%= email %><% } %>" required>
      <input type="text" name="username" placeholder="Username" value="<% if (locals.user) { %><%= user %><% } %>" required>
      <input type="password" name="password" placeholder="Password" required>
      <input type="password" name="passwordconfirm" placeholder="Password-Confirm" required>
      <input type="submit" value="Register">
    </form><br>

    <a href="/login">Login</a>
  </div>
</body>
</html>